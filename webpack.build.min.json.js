const path = require('path');

module.exports = {
  entry: './src/feCharts.ts',
  module: {
    rules: [
      {
        test: /\.tsx?$/,
        use: 'ts-loader',
        exclude: /node_modules/
      }
    ]
  },
  resolve: {
    extensions: ['.ts', '.js']
  },
  output: {
    filename: 'feCharts.min.js',
    path: path.resolve(__dirname, 'dist')
  }
};